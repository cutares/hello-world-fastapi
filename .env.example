# 环境变量示例(提交到 Git,不包含真实 Secret)
# 复制为 .env 并按需修改(注意 .env 已在 .gitignore 中忽略)

# 服务端口(容器内监听 0.0.0.0)
PORT=8000

# 示例 Secret(填真实值时只放 Coolify 环境变量,不提交 Git)
# API_KEY=your-api-key-here
# DATABASE_URL=postgres://user:password@host:5432/dbname
